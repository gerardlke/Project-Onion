Create .env file in root folder with;

```
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=project_onion
POSTGRES_HOST=postgres

DATABASE_URL=postgresql://postgres:password@postgres:5432/project_onion
```